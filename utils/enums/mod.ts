/**
 * @enum {ModNavigationOptions} Navigation enumerations for /mod confirmation buttons
 */
export enum ModNavigationOptions {
    NOTE_CONFIRM    = 300,
    WARN_CONFIRM    = 301,
    MUTE_CONFIRM    = 302,
    BAN_CONFIRM     = 303,
    UNMUTE_CONFIRM  = 304,
    UNBAN_CONFIRM   = 305,
    MAPBAN_CONFIRM  = 306,
    UNMAPBAN_CONFIRM = 307,

    HISTORY_PREV    = 310,
    HISTORY_NEXT    = 311,

    CANCEL          = 999,
}
