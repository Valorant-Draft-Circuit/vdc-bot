// Use strict typing
"use strict";

// Require the modules that we need
// const { ModLogs } = require("@prisma/client");
const { Player, Flags, ControlPanel, Roles } = require("../../../../../prisma");
const { prisma } = require("../../../../../prisma/prismadb");

// Create the function
async function createModLog()
{
    // Need to think more on how we do this....
}