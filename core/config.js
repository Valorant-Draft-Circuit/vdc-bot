const { ActivityType } = require("discord.js");

module.exports = {
    PRESENCE: {
        STATUS: `online`,
        TYPE: ActivityType.Watching,
        MESSAGE: `travestey write bad code`,
    },

    GITHUB : `https://github.com/Unieveth/vdc-bot`,
};
